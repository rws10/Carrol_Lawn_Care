@model IEnumerable<Carrol_Lawn_Care.Models.Tool>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TblEquip.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TblEquip.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.toolId }) |
            @Html.ActionLink("Details", "Details", new { id=item.toolId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.toolId })
        </td>
    </tr>
}

</table>
