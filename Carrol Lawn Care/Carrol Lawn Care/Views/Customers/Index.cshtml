@model IEnumerable<Carrol_Lawn_Care.Models.Customer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TblPer.name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TblPer.name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.custId }) |
            @Html.ActionLink("Details", "Details", new { id=item.custId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.custId })
        </td>
    </tr>
}

</table>
